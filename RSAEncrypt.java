import java.util.Random;
/**
 * Write a description of class RSAEncrypt here.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public class RSAEncrypt
{
    private int x;
    public int randomNumberGen(){
        int number = 0;
        Random randomNumber = new Random(); // generate a random number
        number = randomNumber.nextInt(500);
        /*while (number > 0) {
        if (isPrime(number) != true) {
            number--;
        }
        else{
            return number;
            break;
        }*/
        return 0;
    }}
 
    /*String RSAEncrypt()
    {
        // initialise instance variables
        
        x = 0;
    }*/
 
//}
